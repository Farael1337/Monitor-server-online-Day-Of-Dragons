# Основные библиотеки
discord.py==2.5.1            # Ядро для работы с Discord API (фиксируем стабильную версию)
a2s==0.2.0                   # Для запросов к игровым серверам (A2S)
python-dotenv==1.0.1         # Для загрузки токена из файла .env

# Критичные зависимости discord.py с фиксацией версий
aiohttp==3.12.3              # HTTP-клиент (фиксация для избежания таймаутов)
attrs==23.2.0                # Утилиты для классов (требуется aiohttp)
multidict==6.0.5             # Мультисловари (требуется aiohttp)
yarl==1.9.4                  # URL-библиотека (требуется aiohttp)
frozenlist==1.4.1            # Неизменяемые списки (требуется aiohttp)
async-timeout==4.0.3         # Таймауты для asyncio
charset-normalizer==3.3.2    # Нормализация кодировок
